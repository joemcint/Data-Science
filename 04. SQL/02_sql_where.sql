/* 02. The WHERE clause specifies a condition that acts as a filter. */
        SELECT *
        FROM vendors
        WHERE vendor_id = 19;
    
        /* vendor_id = 19 is the condition. Only shows with an vendor_id equal to 19 will be returned. */
    /*  
        # Comparison Operators
        Here are all the SQL comparison operators that we can use in a condition:
            = equal to
            != not equal to
            <> not equal to
            > greater than
            < less than
            >= greater than or equal to
            <= less than or equal to
    */

/*
https://docs.snowflake.com/en/sql-reference/constructs/where
*/