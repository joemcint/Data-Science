/* 03. LIKE performs a case-sensitive comparison to determine whether a string matches or does not match a specified pattern. */
       SELECT * 
       FROM vendors 
       WHERE vendor_name LIKE '3%'; 
    
/*
https://docs.snowflake.com/en/sql-reference/functions/like
*/
